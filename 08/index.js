let raio = 1;

const comprimento = 2 * Math.PI * raio;
console.log(`Comprimento do circulo = ${comprimento.toFixed(1)}`);

const areaTotal = (Math.PI * (raio ** 2));
console.log(`Área do circulo = ${areaTotal.toFixed(1)}`);//